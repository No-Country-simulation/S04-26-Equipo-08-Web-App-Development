{
  "name": "northpay-backend",
  "version": "1.0.0",
  "description": "NorthPay onboarding backend",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:init": "psql \"%DATABASE_URL%\" -f src/database/init.sql",
    "db:schema": "psql \"%DATABASE_URL%\" -f src/database/schema.sql",
    "db:seed": "psql \"%DATABASE_URL%\" -f src/database/seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
